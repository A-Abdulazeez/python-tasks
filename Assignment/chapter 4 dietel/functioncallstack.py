'''
A stack works in last-in, first-out order. If you keep pushing items onto a stack without popping enough items off, the stack keeps growing.

In programming, if function calls keep adding to the function-call stack without returning, the program can run out of stack space. This may cause a stack overflow or a recursion error.

In Python, this commonly happens with infinite recursion. Python stops the program and raises a RecursionError.
'''

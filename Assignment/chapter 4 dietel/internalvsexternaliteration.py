'''
Internal iteration is preferable in functional-style programming because it allows the programmer to focus on the result instead of the step-by-step loop process.

With internal iteration, functions such as map, filter, reduce, sum, min and max handle the looping internally. This often makes the code shorter, clearer and less error-prone.

External iteration uses explicit loops, so the programmer must control the loop manually. That can make the code longer and easier to get wrong.
'''
